package newsanalyzer.downloader;

import java.util.concurrent.Callable;

public class URLThreaded implements Callable<String> {
    @Override
    public String call() throws Exception {
        return null;
    }
}
